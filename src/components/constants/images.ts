const imageAssetsPath = '/assets/images';
const iconAssetsPath = '/assets/icon';

export const images = {
  defaultImage: `${imageAssetsPath}/rate.jpg`,
};

export const icons = {
  logo: `${iconAssetsPath}/logo.svg`,
};
